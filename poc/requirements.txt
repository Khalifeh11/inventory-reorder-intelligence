streamlit>=1.32
pandas>=2.0
numpy>=1.26
altair>=5.0
anthropic>=0.40  # optional — only used when ANTHROPIC_API_KEY is set
python-dotenv>=1.0  # loads ANTHROPIC_API_KEY from a local .env file
jupyter>=1.0
